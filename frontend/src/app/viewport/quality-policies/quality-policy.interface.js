/**
 * @ngdoc interface
 *
 * @name QualityPolicy
 *
 * @description
 * The qualities policies define the qualities to display in the viewport
 * (and by extension to download, when they are not already cached).
 *
 * Used and instantiated by Viewports based on one of their constructor
 * parameters.
 * 
 * @function QualityPolicy(image): Array
 */
